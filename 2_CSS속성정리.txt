
1. CSS 적용하는 방법
    1. CSS파일 만들어서 HTML <head> 태그에 호출방식[*]
        <link rel="stylesheet" href='css파일경로" >
    2. HTML <head> 태그에 <style> 태그를 이용하는 방식
        <style> </style>
    
    3. HTML <마크업 stlye=""> style 속성을 이용하는 방식